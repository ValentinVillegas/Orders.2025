@if(country is null){
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            @* <CountryForm Country="country" OnValidSubmit="EditAsync" ReturnAction="Return"/> *@
            <FormWithName Label="País: " Model="country" OnValidSubmit="EditAsync" ReturnAction="Return"/>
        </DialogContent>
    </MudDialog>
}